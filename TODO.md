# squadtp-conquest TODO案 (2026-08-06時点)

devlog/READMEの「既知の制約」と重複する項目は載せていない
(squadtp側APIの制約で実装不可能なもの — 蘇生者の正確な特定・戦闘タグクリア・
`ReviveSystem.clear()`のサーバー全体影響 — はdevlog参照)。
実プレイでの動作確認待ちの項目は「未検証」節、詳細な確認ポイントは**HANDOFF.md**の
「既知の問題・積み残し」を参照(ここでは機能名と一言のみ)。

## 優先度高

- **スポーン地点の安全確認**: squadtpの`TeleportHelper.isDestinationDangerous`
  (敵性Mob/敵チームプレイヤーの接近チェック)が既に公開APIとして存在するのに、
  `ConquestManager.teleportToSpawns`では未使用。組み込むだけの小さい修正で、
  湧き待ち撃ち/リスポーンキルをある程度防げる

## 優先度中

- **チーム人数上限/自動バランス**: 現状`/conquest team join`は無制限。`maxTeamSize`config+
  人数偏り時の参加拒否、または参加時に少ない方へ誘導するメッセージ
- **拠点ごとの個別パラメータ**: 現状`captureRadius`/`captureRatePerSecond`は全拠点共通のconfig値。
  拠点によって占領しやすさを変えたい場合(例: 中央拠点だけ広い/速い)は`/conquest point add`に
  半径以外のパラメータ(占領速度倍率など)を持たせる拡張が必要
- **AED専用の管理者トグル**(2026-08-06追加): squadtp本体のAEDは現状`SquadFeature.REVIVE`
  (蘇生システム全体)のトグルにしか従わず、「蘇生は許可するがAEDでの分隊外バイパスだけ禁止したい」
  という運用ができない。squadtp本体の変更が必要なため対応する場合はユーザーに要確認

## 優先度低・思いつき

- **観戦者/未参加者の扱い**: チーム未参加のままラウンドが始まった場合の挙動が未定義気味
  (スポーン移動もされない、HUDも非表示のまま放置される)。強制スペクテイター化や
  「参加してください」の定期リマインドなど
- **拠点名の表示改善**: 現状HUD/GUIのアイコンは拠点名の頭文字1文字のみ。日本語名や絵文字的な
  記号を使いたい場合の見た目調整
- **config値のバリデーション強化**: `/conquest config set`は型チェックのみで範囲チェックをしていない
  (`ForgeConfigSpec`の`defineInRange`範囲を`.set()`が強制するかは未検証)
- **スポットの画面上マーカー**(2026-08-06追加): 現状JourneyMapウェイポイントのみで、BF本編のような
  画面上の頭上マーカー(3D→2D投影)は無い。今回はスコープ外にしたが、squadtp本体の分隊メンバー
  位置共有も同じくJourneyMapのみなので、対応するなら両方に効く共通レンダラーとして作るのもあり
- **チーム別/クラス別のHP差**(2026-08-07追加): `maxHealth`は現状チームA/B共通の一律値。
  攻撃側/防衛側で差をつけたい、または将来クラス制(衛生兵/工兵等、ブレインストーミング済みの案)を
  作るならクラスごとに差をつけたい、といった場合は`applyMaxHealth`をteam/class引数で分岐させる拡張が必要

## 未検証(実プレイテスト)

ビルド成功+`runServer`でのMod読み込み確認までしか行っていない機能が大半。以下は実プレイでの
動作確認が未実施(確認ポイントの詳細はHANDOFF.md参照):

- 待機チーム(2026-08-16新規実装): `/conquest team join waiting`で参加した瞬間インベントリが
  空になるか、通常のサバイバルのまま(スペクテイターモードにならず)移動・ブロック操作ができるか、
  待機チームのプレイヤーが他チームを攻撃してもダメージが通らないか、逆に他チームから待機中の
  プレイヤーを攻撃してもダメージが通らないか、落下ダメージ等の環境ダメージは通常通り受けるか、
  試合のHUD(チケットバー等)が表示されないか、`/conquest team shuffle`の対象から除外されるか
- 拠点制圧のスコア化+チーム別トップ表示(2026-08-15新規実装): 拠点制圧の瞬間に範囲内の制圧側
  プレイヤー全員へスコアが加算されるか(1tickごとの重複加点になっていないか)、範囲外の味方や
  敵チームには加算されないか、MVPとチームA/Bトップが正しくチャットに3行発表されるか、
  片方のチームに参加者がいない場合そちらのトップ発表だけスキップされるか
- 試合終了後の改善(2026-08-15新規実装): ラウンド終了直後、集合地点(または現在地)でPvPダメージが
  実際に無効になるか、次のラウンド開始と同時に元通り有効に戻るか、MVPがチャットに正しく発表される
  か(スコア計算がスコアボードと一致しているか)、参加者が誰もいない状態で終了してもエラーが
  出ないか
- ダウン中の強制蘇生(2026-08-15修正、squadtp本体`ReviveSystem.forceReviveAll`追加): ダウン状態の
  プレイヤーがいる状態でラウンドを終了させ、伏せポーズ・鈍化/発光エフェクトが解除されHPが
  `reviveHealPercent`分回復するか、その後普通に動けるか、蘇生進行中(チャネル中)だった
  もう一方のプレイヤー側の進行バーもおかしくならずに消えるか
- スポーン区域(2026-08-14新規実装): `/conquest spawnzone add`で設定した範囲内でPvPダメージが
  実際に無効になるか(範囲内の被害者への攻撃がキャンセルされるか、範囲外なら通常通りダメージが
  通るか)、範囲内でブロック破壊・設置の両方ができなくなるか(クリエイティブモードは従来通り
  破壊できるか)、黄色いワイヤーフレームで可視化されるか、破壊禁止ゾーンとは独立して動作するか
  (スポーン区域に入っていない破壊禁止ゾーンでは通常通り戦闘・設置ができるか)、
  `/conquest preset save|load`でスポーン区域も保存/復元されるか
- ピン機能(2026-08-14新規実装): `N`キーでクロスヘアの先のブロックにピンが立つか、
  **同じ分隊のメンバーにだけ見えて分隊外(同チームでも)には見えないか**、分隊未所属だと
  自分にしか見えないか、JourneyMapに`pinDurationSeconds`秒表示され自動で消えるか、
  しゃがみ+`N`キーで自分のピンだけ早期に消せるか、新しいピンを立てると前のピンが
  自動的に置き換わるか(1人1ピン)、`pinCooldownSeconds`の連打防止が効くか、
  JourneyMap未導入環境でキーを押してもエラーが出ないか
- ブレイクスルーの攻撃側チケットバー(2026-08-14): 開始直後は満タン表示か、死亡のたびに
  減っていくか、セクター突破ボーナスでバーが一旦満タンに戻るか、割合が100%を超えて
  はみ出さないか
- ブレイクスルーの調整3件(2026-08-14): 分隊内蘇生がブレイクスルー中も機能するか(TDMでは
  引き続き無効のままか)、攻撃側の死亡→リスポーンが待機ウェーブ無しで即座に起きるか、
  `/conquest sector area set`で前後のセクターに戦闘エリアを設定した状態でセクターを突破した際、
  突破前セクターの戦闘エリアに防衛側が侵入すると処刑されるか・次セクターの戦闘エリアに攻撃側が
  侵入すると処刑されるか・セクターが進むたびにこの判定が正しく1つずつスライドするか・
  戦闘エリア未設定のセクターが前後にある場合は何も起きないか
- 第2スポーン地点(2026-08-14新規実装): `/conquest spawn set2 <a|b>`設定後、死亡後リスポーン
  選択画面に「チームスポーン2」が出るか、選ぶと実際にテレポートするか、ラウンド開始時の
  自動テレポートには影響しない(引き続き1個目のみ使われる)か、サーバー再起動後もNBTから
  復元されるか
- GUIの分隊参加リクエスト(2026-08-12新規実装): 状況タブに自チームの他分隊一覧が分隊ごとに
  1行(メンバー名カンマ区切り、リーダー★)で正しく出るか、[参加]ボタンで`/squad join`が送られ
  即時参加/リーダー承認のどちらでも動くか、5件を超える時のスクロールでボタン位置がずれないか。
  別チームのメンバーが混ざった分隊が一覧から正しく除外されるか(2026-08-12追加の`allSameTeam`判定)
- squadtp本体の募集タブ(2026-08-12、squadtp側の変更): 「分隊に参加申請する」欄が分隊単位で
  1行(メンバー名+リーダー★)にまとまっているか、同じ分隊のメンバーが重複して並ばないか、
  既に分隊に入っていても他分隊への切り替え申請ができるか、別チームの分隊が出ないか
- みかんを食べると即死する(2026-08-10新規実装): しゃがみ+右クリックで食べるモーションが出て
  即死するか、通常の右クリックは従来通り投げて着弾破壊するままか
- 管理人チームのHUD表示(2026-08-10新規実装): チケットバー・拠点アイコンが管理人視点で
  正しく表示されるか(チームAが常に左)、ブレイクスルーで攻撃側チケット+次ウェーブ表示になるか
- 観戦者チーム(2026-08-10新規実装): `/conquest team join spectator`でOP以外でも参加でき
  即座にスペクテイターモードになるか、他チームに移るとサバイバルに戻るか、スコアボードの
  「観戦中」欄に表示されるか、HUDが管理人と同様に見えるか
- 演習場(2026-08-10新規実装): `/conquest range set`→`team join range`後の中央テレポート、
  30分自動リセット(`/conquest range reset`での即時実行含む)での地形復元+在室プレイヤーの
  テレポート・全回復、境界処刑/自陣ゾーン処刑/チケット等が`range`チームに一切効かないこと、
  サーバー再起動後のスナップショット自己修復
- 演習場のスポーン地点(2026-08-15新規実装): `/conquest range spawn set`後、`team join range`や
  自動リセット時にエリア中央ではなく指定した地点にテレポートされるか、
  `/conquest range spawn remove`後はエリア中央に戻るか、`/conquest range set`でエリアを
  再設定してもスポーン地点の設定が消えずに残るか
- 複数拠点(2つ以上)でのチケット減少速度の体感バランス
- 開始前カウントダウン中の`/conquest stop`キャンセル
- 開始前カウントダウン中の自陣ゾーン封じ込め(2026-08-15新規実装): 自陣ゾーンを設定した状態で
  ラウンドを開始し、カウントダウン中にゾーンの外へ出ようとすると自チームのスポーン地点へ
  押し戻されるか、警告のアクションバーが出るか、ラウンドが`IN_PROGRESS`に切り替わった瞬間から
  押し戻しが働かなくなるか、自陣ゾーン未設定のチーム/モードでは何も起きないか
- リスポーンチケット消費と蘇生システムの組み合わせ(ダウン→タイムアウト死亡→リスポーンの
  一連の流れでチケットが正しく減るか)
- 拠点範囲パーティクル・旗ブロック(1×4)の実際の見え方バランス
- スコアボードでの分隊メンバー緑ハイライトの表示
- TDMモード(`/conquest mode set tdm`)でのキル数カウント・`tdmKillLimit`到達時の即勝利判定。
  加えて2026-08-15の作り込みで追加した3点: GUIのモード切替ボタンを繰り返し押してconquest→tdm→
  breakthroughの順にちゃんと循環するか、HUDのキル数表示が`現在/目標`(例`12/50`)で正しく出るか
  (`tdmKillLimit`を0にした場合は数値のみに戻るか)、`/conquest status`がTDM中は「Tickets」ではなく
  「Kills」とラベル表示されるか
- ブレイクスルーモード全般(ウェーブリスポーン・セクター前線移動・勝利判定・GUI操作)
- 自陣ゾーン(処刑・両方の座標指定方法・境界の視認性)
- GUIのタブ化(タブ切り替え・セクタータブの半径入力・モード切替時の挙動)
- 地形破壊(クレーター生成・破壊禁止ブロック/エリア・ラウンド開始時の復元・大量爆発時の負荷)
- ゾーンワンド(左右クリックでの角記録・座標省略コマンドとの連携)
- 戦場境界(処刑・自陣ゾーンとの重複時のメッセージ)
- セクター別戦闘エリア(境界判定・突破直後の猶予・グローバル境界へのフォールバック)
- セクター突破チケットボーナス・コールイン(付与タイミング・GUI操作・スコア消費)
- チームリスポーンビーコン(テレポート・自動消滅・置き換え)
- チームビーコン/拠点スポーンの選択UI化(squadtpのリスポーン選択画面での表示・テレポート・
  分隊未所属での表示・係争中/被占領中の拠点が選択肢から消えるか)
- squadtpのAED追加に伴う副作用(分隊未所属もダウンするようになったこと・AEDでの分隊外蘇生・
  クールダウン)
- 拠点のJourneyMapウェイポイント表示(表示・チーム色・ラウンド終了/ログアウトでの消滅・
  JourneyMap未導入環境での無害性)
- 索敵マーキング(スポット)(反応・壁越し不可・クールダウン・自動消滅)。2026-08-09、実プレイで
  ユーザーから「スポットが出来ない」「敵の位置がもう見えている」と報告 → スポットのコード自体は
  正常だったが、JourneyMapのレーダー機能がスポット無しでも敵を常時表示していたのが原因と判明。
  新規`ConquestJmRadarEvents`(`EntityRadarUpdateEvent`をフックし敵チームをレーダーから除外)で
  対処(README参照)。**次回確認が必要な点**: 実プレイでレーダーから敵チームが消え、味方は
  従来通り映るか。スポットした敵がWaypoint経由で問題なく見えるか(レーダー抑制と競合しないか)。
  JourneyMap未導入環境で無害か(`ConquestJmPlugin`経由の登録なのでロードされないはず)
- 試合終了時の集合(テレポート発生・安全な着地・観戦チーム対象外)
- 破壊禁止ブロックのゲーム内管理(追加/削除が実際に効くか・config既定分は解除不可なこと・
  再起動後もNBTから復元されるか)
- 地形の自動復元(スナップショット方式、`/conquest boundary`が対象範囲): 境界内の破壊が
  ラウンド終了時に確実に元通りになるか・境界外は対象外のままか・境界が広い場合のラグ
- ブレイクスルーでの地形自動復元フォールバック(2026-08-14新規実装): グローバル境界を設定せず
  `/conquest sector area set`だけを設定した状態でラウンドを回し、全セクターの戦闘エリアを
  合算した範囲が正しくスナップショットされ、ラウンド終了時に元通りになるか。戦闘エリア未設定の
  セクターが混ざっていても残りのセクターだけで正しく合算されるか
- 最大HPのconfig化(`maxHealth`): チーム参加時/ラウンド開始時/リスポーン時の反映・管理人チームでバニラに
  戻るか(リスポーン時の反映は2026-08-08追加、実プレイ未検証)
- 破壊禁止ブロック/エリアの通常破壊からの保護: サバイバルで壊せないか・クリエイティブは壊せるか・
  ラウンド状態に関係なく常時有効か
- SuperbWarfareのRPG等、`maxBlocksPerExplosion`を超える大きい爆発でも破壊禁止ブロック/エリアが
  実際に守られるようになったか(2026-08-07修正、判定順序のバグ対処)
- TNTの誘爆: ラウンド進行中、TNT複数個をまとめて設置して1つを起爆し、隣接TNTが連鎖的に誘爆する
  (クレーター化されても)か(2026-08-08修正、`wasExploded`呼び出し漏れの対処)
- `/conquest team join`で個別にチームを切り替えた後、元squadメンバー(現在は敵)をAEDで蘇生できなく
  なったか(2026-08-08修正、`joinTeam`での`leaveSquadIfAny`追加によるバグ対処)
- 死亡後のsquadtpリスポーン選択画面に「チームスポーン」が表示され、`/conquest spawn set`の位置へ
  実際にテレポートするか(2026-08-08修正、`ConquestRespawnChoiceProvider`への選択肢追加)
- みかん(`squadtpconquest:mikan`): 投げて当たったブロックが実際に破壊されるか、破壊禁止ブロック/
  エリアには効かないままか(2026-08-08新規実装)
- `/conquest boundary restore`: `/conquest start`→地形破壊→`/conquest stop`の後に実行して、
  実際に地形が元に戻るか。スナップショットが無い状態(境界未設定・`endRound`後)で失敗メッセージが
  出るか(2026-08-08新規実装)。**同じラウンド内で「壊す→復元→また壊す→また復元」を2回以上
  繰り返しても毎回正しく効くか**(2026-08-15修正、以前は1回で消費されていた)。
  **実行した瞬間にチャット警告が流れ、`terrainRestoreDelaySeconds`秒後(既定5秒)まで実際には
  復元されないか**(2026-08-15新規実装、`/conquest range reset`も同様)
- `/conquest team join <team> <プレイヤー>`: OP以外が末尾のプレイヤー引数付きで実行しようとした際に
  権限エラーになるか(タブ補完にも出ないはず)。対象プレイヤーに参加通知のアクションバーが出るか
  (2026-08-08新規実装)。**`@a`で全員を一括参加させられるか**(2026-08-15追加)、その際確認メッセージの
  人数表示が正しいか、対象者全員にアクションバー通知が届くか
- プリセットのゾーン/破壊禁止ブロック保存/復元: 自陣ゾーンA/B・戦場境界・破壊禁止ゾーン・
  `/conquest protectblock add`で追加したブロックを設定した状態で`/conquest preset save`→
  (すべて`/conquest zone|boundary|protectzone remove`・`protectblock remove`で消してから)
  `/conquest preset load`し、元通りに復元されるか。config既定の`indestructibleBlocks`は
  プリセットに影響されず常に保護されたままか(2026-08-08新規実装)
- 新規ワールドで`/conquest preset list`に既定で`Normal`(まっさらな状態のプリセット)が
  出るか、`load`すると拠点・スポーン・ゾーン・破壊禁止ゾーンが全部消えるか(2026-08-08新規実装、
  既存ワールドには遡って追加されない仕様なので新規ワールドで確認すること)
